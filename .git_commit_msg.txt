feat: replace TMDB with OMDB & TVMaze, fix nav overlap

- Replaced TMDB API (which is blocked by some Indian ISPs) with OMDb API for movies and TVMaze for TV shows to ensure reliable search functionality.
- Added safe area insets to the bottom tab bar to prevent icons from overlapping with the system navigation bar on modern Android devices.
