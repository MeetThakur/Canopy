feat: calm ui, local search, and progress tracking

- Installed `expo-haptics` for tactile feedback
- Completely redesigned Homepage (`index.tsx`) to a calm, minimalist layout with gentle lists instead of massive blocks
- Updated Library (`library.tsx`) with a refined header, local offline Search Bar, and A-Z/Newest/Best sorting options
- Revamped Detail Screen (`[id].tsx`) to be stark and clean, integrating granular progress trackers (pages/episodes/hours) with haptic feedback
